# Changelog

## 1.1.0

- Add exact MCF 1.0/1.1 version dispatch.
- Add all MCF 1.1 package kinds, question kinds, activities, structured
  diagnostics, extensions, relationships, question banks, and closed package
  sets.
- Add restricted MCF YAML parsing and bounded secure `.mcf.zip` extraction.
- Add `inspect` and `capabilities` commands plus JSON diagnostics and stable
  exit codes.
- Compile MCF 1.1 courses in directory and standalone modes, including declared
  `asset:` references.
- Synchronize the browser reader with the current `mcf-npm` implementation.
- Add canonical principal-suite and data-driven conformance tests.
