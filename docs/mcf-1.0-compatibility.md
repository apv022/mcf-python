# MCF 1.0 compatibility

Exact `1.0` manifests use only the 1.0 schema/parser/defaults, from directory or
safe archive transport. They are never relabeled. Authored ordering and all six
1.0 question types are retained; essays remain manual in the static reader.
