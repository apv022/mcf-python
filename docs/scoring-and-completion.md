# Scoring and completion

The shared runtime supplies pure, type-aware completeness and scoring for all
nine types. Manual work yields a provisional score and never correctness;
ungraded/completion work stays outside objective denominators. Nested `all` and
`any` expressions, target states, submitted/passed distinction, and
`minimum_score` are evaluated at runtime. See the Node document for the exact
fallback policy shared by both outputs.
