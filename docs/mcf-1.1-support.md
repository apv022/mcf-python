# MCF 1.1 support

All five kinds and nine question types parse and validate. Courses, modules,
and lessons compile; banks/assets participate in package sets but do not produce
meaningless HTML. YAML restrictions, assets, rubrics, completion, references,
metadata, and extensions are retained. No conformance class is advertised.
